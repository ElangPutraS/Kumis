<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.uii.a16523169students.kumis">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/geasy"
        android:label="@string/app_name"
        android:logo="@drawable/geasy"
        android:roundIcon="@drawable/geasy"
        android:supportsRtl="true"
        android:theme="@style/GeasyStyle">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Input1Activity"
            android:label="Activity Home"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="id.ac.uii.a16523169students.kumis.MainActivity" />
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".KonsultasiActivity" />
        <activity android:name=".RegisterSSOActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".CekSehatActivity" />
        <activity android:name=".HomeActivity" />
    </application>

</manifest>